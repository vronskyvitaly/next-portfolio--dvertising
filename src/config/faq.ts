import { CATALOG_ROW, formatPrice, getService } from './services'
import type { ServiceFaqItem } from './services'

/**
 * FAQ главной страницы. Один источник для src/components/Faq.tsx и для
 * FAQPage-разметки в src/app/page.tsx — раньше текст дублировался и разъезжался.
 *
 * Цены собираются из реестра услуг, а не пишутся руками.
 */

const lending = getService('lending')!
const corporate = getService('korporativnyy-sayt')!
const shop = getService('internet-magazin')!
const webapp = getService('veb-prilozhenie')!

const priceLine = [
  `лендинг — ${formatPrice(lending.priceFrom)}`,
  `корпоративный сайт — ${formatPrice(corporate.priceFrom)}`,
  `каталог — ${formatPrice(CATALOG_ROW.priceFrom)}`,
  `интернет-магазин — ${formatPrice(shop.priceFrom)}`,
  `веб-приложение — ${formatPrice(webapp.priceFrom)}`
].join(', ')

const durationLine = [
  `лендинг — ${lending.duration}`,
  `корпоративный сайт — ${corporate.duration}`,
  `интернет-магазин — ${shop.duration}`,
  `веб-приложение — ${webapp.duration}`
].join(', ')

export const homeFaq: ServiceFaqItem[] = [
  {
    q: 'Сколько стоит разработка сайта?',
    a: `Зависит от типа: ${priceLine}. Точную цену назову после разбора задачи и зафиксирую в договоре. Подробный разбор цен и того, что входит в каждый вариант, — на странице «Создание сайтов под ключ».`
  },
  {
    q: 'Как долго занимает разработка?',
    a: `Ориентиры: ${durationLine}. Сроки фиксируем в договоре. Растут они обычно по одной причине — тексты и фотографии собираются уже в процессе.`
  },
  {
    q: 'Что такое автоматизация бизнеса и как она помогает?',
    a: 'Это перевод ручных повторяющихся задач в автоматический режим: автоматическая выгрузка отчётов, уведомления клиентам, обработка заявок, интеграция сервисов. В среднем экономит 2–5 часов работы в день.'
  },
  {
    q: 'Как внедряется ИИ в организацию?',
    a: 'Анализирую процессы компании, нахожу задачи, где ИИ даёт эффект. Затем настраиваю и интегрирую: ИИ-ассистент для сотрудников, автоматическая обработка входящих, генерация документов и ответов.'
  },
  {
    q: 'Работаете ли вы с небольшими компаниями?',
    a: 'Да. Работаю как с малым бизнесом и ИП, так и со средними компаниями. Решения подбираю под реальный бюджет и задачи.'
  },
  {
    q: 'Остаётесь ли на связи после завершения проекта?',
    a: 'Да. После сдачи проекта включается период поддержки. Мелкие правки — бесплатно. Доработки и новые задачи — по договорённости.'
  }
]
